pragma language_version >= 0.14.0;
import CompactStandardLibrary;
export ledger highest: Uint<64>;
export circuit test_cond(amount: Uint<64>): [] {
  const curr = highest;
  if (amount > curr) {
    highest = amount;
  }
}

node_modules/
dist/
*.log
.env
.DS_Store

# managed/node_modules is a symlink to mn-demo/node_modules created at runtime
# to ensure the compiled contract resolves @midnight-ntwrk/* from a single
# WASM instance. Do not commit it.
managed/node_modules
managed/package.json

# Binary ZK intermediate representation files (not human-readable)
*.bzkir
